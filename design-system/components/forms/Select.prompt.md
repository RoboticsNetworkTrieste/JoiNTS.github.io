Native `<select>`, restyled. Prefer it over a custom dropdown — it is keyboard- and mobile-correct for free.

```jsx
<Select placeholder="Tutti gli ambiti" options={['SLAM','Manipolazione','Percezione','Controllo']} />
```
