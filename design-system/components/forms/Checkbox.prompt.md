Checkbox / radio with an 18px box, 1.5px border, accent fill when on.

```jsx
<Checkbox label="Voglio ricevere gli inviti ai meetup" defaultChecked />
<Checkbox type="radio" name="ruolo" label="Studente" />
```

Stack groups in a flex column with `gap: var(--space-3)`.
