Wraps any control with its mono uppercase label, hint and error.

```jsx
<Field label="Email" htmlFor="email" required hint="Non la condividiamo con nessuno.">
  <Input id="email" type="email" placeholder="nome@dominio.it" />
</Field>
```

Labels are 1–3 words, no colon. Hints are full sentences.
