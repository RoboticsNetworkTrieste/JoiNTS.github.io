Text input on an inset (`--surface-3`) fill with a hairline border; focus adds an accent border plus a 3px accent-quiet halo.

```jsx
<Input placeholder="Cerca progetti" icon="search" />
<Input mono placeholder="https://github.com/…" />
<Input invalid defaultValue="nome@" />
```
