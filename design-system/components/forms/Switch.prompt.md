Use for immediate-effect settings; use Checkbox inside forms that need submitting.

```jsx
<Switch label="Mostra solo progetti attivi" defaultChecked />
```

The knob travels 18px on a 140ms servo easing — no bounce.
