Shows real commands and real code — the site's proof that things run.

```jsx
<CodeBlock language="bash">{'git clone https://github.com/joints-trieste/harbor-slam\ncolcon build --symlink-install'}</CodeBlock>
```

Keep snippets under ~8 lines. Highlight sparingly with `<span className="k">`, `"s"`, `"c"`.
