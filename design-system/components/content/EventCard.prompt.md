Lists upcoming in-person things. Stack them in a flex column with `gap: var(--space-3)`.

```jsx
<EventCard day="18" month="set" kind="meetup" title="Demo night: bracci e presa"
  where="Area Science Park" when="18:30 → 21:00" href="/eventi/demo-night" />
```
