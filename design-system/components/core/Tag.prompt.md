Rounded mono pill used for topics and filter rows on the projects index.

```jsx
<Tag onClick={fn} selected count={7}>slam</Tag>
<Tag>manipolazione</Tag>
```

Lay tag rows out with flex + `gap: var(--space-2)`.
