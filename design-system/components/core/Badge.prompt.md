Mono, uppercase, 22px-tall status chip. For machine facts (state, licence, version), not for navigation.

```jsx
<Badge tone="ok" icon="circle-check">attivo</Badge>
<Badge tone="accent">ros 2 jazzy</Badge>
<Badge>apache-2.0</Badge>
```

One or two words, lowercase in source (CSS uppercases it).
