Closes every page. Deep-ink ground (`--bg-page-deep`) so it reads as the base of the document.

```jsx
<SiteFooter
  note="Associazione senza scopo di lucro. Tutto ciò che creiamo è open source."
  columns={[{ title:'Community', links:[{label:'Manifesto',href:'/manifesto'}] }]}
  meta={['CC BY-SA 4.0 · codice Apache-2.0','Trieste, Italia','2026']}
/>
```
